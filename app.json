{
  "name": "End Kang Bot",
  "description": "pyrogram ub to save your mentions and dms",
  "logo": "https://te.legra.ph/file/ac72a0208034b15f04c18.jpg",
  "keywords": [
    "telegram",
    "bot",
    "python",
    "pyrogram"
  ],
    "buildpacks": [{
    "url": "heroku/python"
  }],
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "free"
    }
  },
  "addons": [
      {
         "options": {
            "version": "12"
         },
         "plan": "heroku-postgresql"
      }
   ],
  "repository": "https://github.com/theend-alpha/StringBot",
  "env": {
    "ENVIRONMENT": {
      "description": "Don't change ANYTHING.",
      "required": true,
      "value": "ANYTHING"
    },
    "API_ID": {
      "description": "Get this value from my.telegram.org.",
      "required": true,
      "value": ""
    },
    "API_HASH": {
      "description": "Get this value from my.telegram.org.",
      "required": true,
      "value": ""
    },
    "STRING_SESSION": {
      "description": "Obtain a pyrogram string session",
      "required": true,
      "value": ""
    },
    "SUDO_USERS": {
      "description": "GET IT FROM @Timeisnotwaiting",
      "required": true,
      "value": ""
    },
    "BOT_TOKEN": {
      "description": "GET IT FROM BOTFATHER",
      "required": true,
      "value": ""
    }
  }
}
